# Known Assumptions And Gaps

## Purpose
This file makes uncertainty explicit so future sessions do not accidentally convert missing information into invented product behavior.

## Active Items
| ID | Type | Statement | Impact | Required Follow-Up | Status |
| --- | --- | --- | --- | --- | --- |
| GAP-001 | Gap | No iOS screen and flow reference package exists in this repo. | Screen inventory and UI parity work are blocked. | Audit the iOS app and populate the inventory docs. | Open |
| GAP-002 | Gap | No approved design token export, icon catalog, or motion spec exists in this repo. | Production design system work is blocked; starter theme is not product truth. | Export or document parity-approved design assets from iOS. | Open |
| GAP-003 | Confirmed Baseline | The approved provisional Learn runtime spec is now documented for the current Android scope: category list `id` / `title`, detail array items `id` / `title` / `body`, and derived `jpg` / `mp4` URLs keyed by category id. | Learn domain/data mapping can now be implemented without guessing beyond those fields. | Expand Learn only if the runtime contract or sourced product docs prove more fields. | Closed |
| GAP-004 | Confirmed Baseline | The Android app now has a modular foundation and placeholder navigation shell, but no sourced feature implementation. | Production features remain blocked on source intake. | Replace placeholders only as sourced feature work begins. | Open |
| GAP-005 | Confirmed Baseline | Hilt, Room, Retrofit, OkHttp, Kotlinx Serialization, Navigation Compose, Coil, and modularization are integrated at foundation level only. | Shared infrastructure exists, but real feature/data behavior is still missing. | Add source-backed implementations incrementally. | Open |
| GAP-006 | Assumption | Until source material is imported, the safest course is to freeze feature implementation and focus on documentation plus minimal bootstrap only. | Prevents unsourced product invention. | Reassess after source inventory is complete. | Active |
| GAP-007 | Assumption | The current starter dynamic color behavior must not be treated as production behavior. | Avoids visual parity drift. | Disable or replace once real design tokens are sourced. | Active |
| GAP-008 | Technical Decision | The build is pinned to AGP `8.13.2` and Gradle `8.13` to keep API `36.1` support while maintaining a buildable Hilt baseline. | Future toolchain upgrades must re-validate Hilt compatibility before changing AGP major versions. | Reassess when Hilt officially supports the next AGP major line used by the project. | Active |
| GAP-009 | Visual Assumption | The shared Compose design system uses provisional semantic color, spacing, shape, gradient, and surface values based on generic iOS-like visual conventions because no parity-approved visual export exists in the repo. | The design system architecture is reusable, but its values are not final product truth. | Replace provisional values after the iOS visual audit and asset export are available. | Active |
| GAP-010 | Visual Assumption | Typography currently uses a temporary system sans-serif hierarchy because no approved iOS font export or text-style mapping exists in the repo. | Text scale and weight structure are stable, but exact font parity is still unresolved. | Replace with sourced typography tokens once the iOS reference is imported. | Active |
| GAP-011 | Component Assumption | Shared `core/ui` components use slot-based icon/content APIs and neutral preview glyphs because no parity-approved icon set or component-level iOS screenshots are present in the repo. | The reusable component structure is in place, but exact iconography and some visual density details remain provisional. | Replace preview placeholders and refine component chrome after iOS component references and icon assets are imported. | Active |
| GAP-012 | Domain Assumption | Habit quantity values, target values, and default increments are currently modeled and persisted as `Double` because no sourced integer-versus-decimal precision rule exists yet. | If the iOS product constrains quantity precision differently, Room schema and mappers may need a migration. | Confirm numeric precision and rounding rules from the iOS product before feature logic depends on these values. | Active |
| GAP-013 | Domain Assumption | Mood entries currently persist a generic `moodToken` string because no sourced mood taxonomy, icon mapping, or rating scale exists in the repo. | The storage layer is ready, but final mood-selection UI and analytics semantics are still blocked. | Import the iOS mood vocabulary and storage rules before implementing mood feature behavior. | Active |
| GAP-014 | Domain Assumption | The settings foundation currently stores only a global `reminderNotificationsEnabled` flag as the smallest settings contract tied directly to the sourced-adjacent reminder concept. | Settings persistence is structurally ready, but the settings domain is not parity-final. | Replace or expand the settings model only after the iOS settings inventory and rules are imported. | Active |
| GAP-015 | Domain Assumption | The profile summary model currently exposes only aggregate counts derivable from local habit, entry, mood, and reminder tables because no sourced profile metric or identity contract exists. | Profile repository structure exists, but profile content parity is still blocked. | Confirm the iOS profile summary fields before wiring the repository into presentation code. | Active |
| GAP-016 | Confirmed Baseline | `LearnRepository` now aggregates `index.json` plus `{categoryId}.json` and no longer returns an unconditional `Unavailable` placeholder. | Runtime Learn content can now reach the current Learn feature through the repository boundary. | Revisit only if the Learn backend contract changes or local persistence/caching becomes a sourced requirement. | Closed |
| GAP-017 | Home Assumption | The Home habits list currently includes only `ACTIVE` habits scheduled for the selected date, and treats an empty selected-days set as an every-day fallback until sourced schedule semantics exist. | The Home MVI logic is buildable and testable, but final list membership rules may need adjustment once the iOS Home screen rules are known. | Verify Home schedule filtering behavior from the iOS product before wiring parity UI or analytics. | Active |
| GAP-018 | Home Assumption | Home mood selection currently models one effective mood choice per date by reusing the first observed same-day mood entry id, and Home snapshot state also uses the first observed same-day mood token unless no entry exists and a deterministic date-scoped id must be created. | The Home mood state is deterministic, but it depends on repository ordering and may differ from the real product if multiple moods per day are supported. | Confirm iOS Home mood-entry rules and ordering semantics before expanding or changing mood interaction behavior. | Active |
| GAP-019 | Home Assumption | When `allowsMultipleUpdatesPerDay` is `false`, Home treats boolean habits as one-tap-per-day no-op after the first entry and quantity habits as single-entry-per-day replacement using a date-scoped entry id. | Home quick actions now honor the existing domain flag, but exact repeat-tap behavior is still provisional. | Verify repeat-update semantics from the iOS product before final UI wiring. | Active |
| GAP-020 | Home Assumption | Home greeting state currently derives `MORNING`, `AFTERNOON`, and `EVENING` from local-hour buckets `5..11`, `12..16`, and all remaining hours because no sourced greeting-time rule exists in the repo. | Greeting logic is deterministic and testable, but the actual product buckets and copy behavior may differ. | Confirm greeting time-of-day rules from the iOS Home screen before wiring the ViewModel into presentation. | Active |
| GAP-021 | Home UI Assumption | Until verified iOS Home references are imported, the Home UI uses provisional visual and copy fallbacks: a read-only mood summary control, a 7-day selected-date-centered date strip, token-or-name monogram habit icons, semantic accent mapping for color tokens, text-monogram bottom navigation items, provisional English copy, and no visible edit action when no real edit flow is wired. | The Home screen is buildable and avoids exposing unresolved navigation, but visual parity and some affordance details remain provisional. | Import iOS Home screenshots and flow references, then reconcile copy, iconography, color usage, mood treatment, date-strip range, bottom-navigation visuals, and action-sheet options. | Active |
| GAP-022 | Create Habit Assumption | Create Habit currently starts in boolean mode, applies quantity validation only when quantity mode is selected, supports only `EVERY_DAY`, `WEEKDAYS`, `WEEKENDS`, and `CUSTOM` schedule presets, preserves the current visible day selection when entering custom mode, and normalizes blank quantity unit labels to `null`. | The Create Habit logic is deterministic and testable, but its defaults and schedule-editing behavior may diverge from the iOS product once the sourced Create Habit flow is audited. | Verify Create Habit defaults, schedule presets, and weekday-editing behavior from the iOS product before wiring the ViewModel into presentation. | Active |
| GAP-023 | Create Habit Assumption | Create Habit currently treats icon and color selection as required token picks despite the missing approved catalog, gives newly added reminders a provisional default time of `09:00`, maps reminder schedules to the habit-level selected days, and saves by persisting the habit first and reminders second with a best-effort rollback if a reminder save fails because no transactional create-habit contract exists yet. | The Create Habit save flow is buildable and testable, but picker defaults, reminder behavior, and failure semantics may need to change once the iOS Create Habit flow and persistence rules are sourced. | Verify Create Habit picker behavior, reminder rules, and create-save transaction semantics from the iOS product before wiring the ViewModel into presentation or finalizing repository contracts. | Active |
| GAP-024 | Create Habit UI Assumption | Until verified iOS Create Habit references are imported, the Create Habit UI uses provisional English section copy, a single full-screen form, a modal goal sheet, full-screen icon and color picker screens with a single `Available` group and monogram/color-token placeholders, picker selection that stays on the picker until manual back navigation, and reminder rows that expose add/remove/enable state but keep reminder time read-only at the current value. | The Create Habit route is now buildable and testable, but picker grouping, copy, reminder-time editing treatment, and some flow details may diverge from the iOS product. | Import iOS Create Habit screenshots, picker references, and reminder interaction rules, then reconcile screen structure, picker dismissal behavior, copy, asset grouping, and reminder-time editing UI before treating the feature as parity-final. | Active |
| GAP-025 | Activity Assumption | Activity currently defaults to the `WEEKLY` period anchored on the current local date, uses ISO-style Monday-through-Sunday weekly windows plus calendar-month monthly windows, keeps chart points day-granular for daily/weekly/monthly views, and does not impose a future-period navigation bound because no sourced Activity calendar semantics exist in the repository. | The Activity logic is deterministic and testable, but its initial period, window rules, chart granularity, and next-period behavior may diverge from the iOS product once the Activity audit exists. | Verify Activity default period, period-window rules, chart granularity, and next-period navigation constraints from the iOS reference before wiring the ViewModel into presentation. | Active |
| GAP-026 | Activity Assumption | Activity analytics currently derive average completion from capped per-habit completion ratios across scheduled habit-days, treat `doneDays` for the all-habits filter as dates where every scheduled filtered habit met its current target, expose all stored habits as filter options, keep zero-entry periods as content with zero metrics when habits exist, inherit the current empty `selectedDays` => every-day schedule fallback used elsewhere in the app, and use the current habit target/schedule/createdAt/stoppedAt metadata across historical dates because no versioned habit-history model exists. | The Activity analytics layer is buildable and directly tested, but completion semantics, filter scope, empty-versus-zero treatment, schedule fallback semantics, and historical accuracy may need to change once the iOS Activity rules are sourced. | Verify Activity completion semantics, done-day rules, filter scope, empty-state rules, schedule fallback behavior, and historical habit-version behavior from the iOS product before wiring the ViewModel into UI or extending repository contracts. | Active |
| GAP-027 | Activity UI Assumption | Until verified iOS Activity references are imported, the Activity UI uses provisional English copy, a plain title-based top section, a tinted current-period header card with explicit date-range text, a single-choice habit filter bottom sheet that dismisses after selection, and a simple day-granular vertical bar chart rendered directly from the existing `chartData` with no extra gestures, overlays, or bottom-navigation treatment. | The Activity route is buildable and testable, but chart visuals, control styling, filter-sheet behavior, copy, and surrounding navigation chrome may diverge from the iOS product. | Import iOS Activity screenshots, chart states, filter-sheet references, and navigation chrome references, then reconcile the Activity header treatment, chart presentation, filter-sheet behavior, copy, and surrounding screen chrome before treating the feature as parity-final. | Active |
| GAP-028 | Confirmed Baseline | The shared Learn model surface now exposes only the runtime-proven fields needed by the current Learn UI: category `id`, category `title`, derived `imageUrl`, derived `videoUrl`, and detail `sections(id, title, body)`. | Learn mapping stays conservative and explicit, but broader Learn semantics remain blocked on source material. | Expand the Learn model only when additional fields are proven by the backend contract or iOS/product docs. | Closed |
| GAP-029 | Profile Blocker | The current Profile surface is still a placeholder route plus a provisional `ProfileSummary` contract that exposes only aggregate counts; the repository has no sourced Profile screen inventory, identity/avatar fields, change-photo behavior, settings-shortcut behavior, period switching rules, or mood analytics spec. | Profile MVI and UI cannot be implemented without inventing screen structure, copy, data fields, and analytics behavior that are not backed by the iOS product. | Import the real Profile screen inventory and source-backed Profile contract first, then expand the Profile model surface only to the verified fields and states required by the iOS Profile screen. | Active |
| GAP-030 | Learn UI Assumption | Until verified Learn references are imported, the Learn feature uses a conservative title-first grid/detail UI: runtime detail items are rendered as titled paragraph cards, derived media URLs are surfaced directly, and video playback still depends on a follow-up app integration decision. | The Learn route is buildable and testable, but card chrome, detail structure, copy, media treatment, and final video behavior may diverge from the iOS product. | Import the real Learn screen inventory, then reconcile the Learn UI and either wire or suppress the video CTA based on the approved app-level playback path. | Active |
| GAP-031 | Confirmed Baseline | The Learn data binding now stays app-owned: `data:learn` remains the repository/Hilt binding owner and `feature:learn` no longer depends directly on `data:learn`. | The Learn module graph now matches the architecture rule that features depend on contracts/core only while the app owns feature/data assembly. | Revisit only if Learn wiring moves to a different app-owned integration path later. | Closed |
| GAP-032 | Learn Integration Gap | The Learn runtime data path now exposes derived `videoUrl`, but the current app shell still passes the default no-op `onVideoRequested` callback into `LearnRoute`. | Learn detail can expose a dead `Play Video` CTA even though the repository data is now real. | Either wire `LearnEffect.OpenVideo` to a real launcher in app/navigation or suppress the CTA until that launcher exists. | Active |

## Update Rule
- Add a new row whenever a session uncovers missing information that can affect implementation.
- Close a row only when the blocking information is actually present in the repository or explicitly approved in durable project docs.
