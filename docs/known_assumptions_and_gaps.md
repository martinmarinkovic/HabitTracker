# Known Assumptions And Gaps

## Purpose
This file makes uncertainty explicit so future sessions do not accidentally convert missing information into invented product behavior.

## Active Items
| ID | Type | Statement | Impact | Required Follow-Up | Status |
| --- | --- | --- | --- | --- | --- |
| GAP-001 | Gap | No iOS screen and flow reference package exists in this repo. | Screen inventory and UI parity work are blocked. | Audit the iOS app and populate the inventory docs. | Open |
| GAP-002 | Gap | No approved design token export, icon catalog, or motion spec exists in this repo. | Production design system work is blocked; starter theme is not product truth. | Export or document parity-approved design assets from iOS. | Open |
| GAP-003 | Gap | No backend or Learn API contract exists in this repo. | Network-layer implementation is blocked. | Import endpoint specs or otherwise verified contract details. | Open |
| GAP-004 | Confirmed Baseline | The Android app now has a modular foundation and placeholder navigation shell, but no sourced feature implementation. | Production features remain blocked on source intake. | Replace placeholders only as sourced feature work begins. | Open |
| GAP-005 | Confirmed Baseline | Hilt, Room, Retrofit, OkHttp, Kotlinx Serialization, Navigation Compose, Coil, and modularization are integrated at foundation level only. | Shared infrastructure exists, but real feature/data behavior is still missing. | Add source-backed implementations incrementally. | Open |
| GAP-006 | Assumption | Until source material is imported, the safest course is to freeze feature implementation and focus on documentation plus minimal bootstrap only. | Prevents unsourced product invention. | Reassess after source inventory is complete. | Active |
| GAP-007 | Assumption | The current starter dynamic color behavior must not be treated as production behavior. | Avoids visual parity drift. | Disable or replace once real design tokens are sourced. | Active |
| GAP-008 | Technical Decision | The build is pinned to AGP `8.13.2` and Gradle `8.13` to keep API `36.1` support while maintaining a buildable Hilt baseline. | Future toolchain upgrades must re-validate Hilt compatibility before changing AGP major versions. | Reassess when Hilt officially supports the next AGP major line used by the project. | Active |
| GAP-009 | Visual Assumption | The shared Compose design system uses provisional semantic color, spacing, shape, gradient, and surface values based on generic iOS-like visual conventions because no parity-approved visual export exists in the repo. | The design system architecture is reusable, but its values are not final product truth. | Replace provisional values after the iOS visual audit and asset export are available. | Active |
| GAP-010 | Visual Assumption | Typography currently uses a temporary system sans-serif hierarchy because no approved iOS font export or text-style mapping exists in the repo. | Text scale and weight structure are stable, but exact font parity is still unresolved. | Replace with sourced typography tokens once the iOS reference is imported. | Active |
| GAP-011 | Component Assumption | Shared `core/ui` components use slot-based icon/content APIs and neutral preview glyphs because no parity-approved icon set or component-level iOS screenshots are present in the repo. | The reusable component structure is in place, but exact iconography and some visual density details remain provisional. | Replace preview placeholders and refine component chrome after iOS component references and icon assets are imported. | Active |
| GAP-012 | Domain Assumption | Habit quantity values, target values, and default increments are currently modeled and persisted as `Double` because no sourced integer-versus-decimal precision rule exists yet. | If the iOS product constrains quantity precision differently, Room schema and mappers may need a migration. | Confirm numeric precision and rounding rules from the iOS product before feature logic depends on these values. | Active |
| GAP-013 | Domain Assumption | Mood entries currently persist a generic `moodToken` string because no sourced mood taxonomy, icon mapping, or rating scale exists in the repo. | The storage layer is ready, but final mood-selection UI and analytics semantics are still blocked. | Import the iOS mood vocabulary and storage rules before implementing mood feature behavior. | Active |
| GAP-014 | Domain Assumption | The settings foundation currently stores only a global `reminderNotificationsEnabled` flag as the smallest settings contract tied directly to the sourced-adjacent reminder concept. | Settings persistence is structurally ready, but the settings domain is not parity-final. | Replace or expand the settings model only after the iOS settings inventory and rules are imported. | Active |
| GAP-015 | Domain Assumption | The profile summary model currently exposes only aggregate counts derivable from local habit, entry, mood, and reminder tables because no sourced profile metric or identity contract exists. | Profile repository structure exists, but profile content parity is still blocked. | Confirm the iOS profile summary fields before wiring the repository into presentation code. | Active |
| GAP-016 | Blocker | `LearnRepository` is intentionally compile-safe but returns `Unavailable` because no verified Learn feature contract or transport/local content spec exists in the repo. | Learn feature implementation remains blocked even though the repository boundary now exists. | Import the Learn product and API/local-content contract before adding DTOs, endpoints, or feature logic. | Active |
| GAP-017 | Home Assumption | The Home habits list currently includes only `ACTIVE` habits scheduled for the selected date, and treats an empty selected-days set as an every-day fallback until sourced schedule semantics exist. | The Home MVI logic is buildable and testable, but final list membership rules may need adjustment once the iOS Home screen rules are known. | Verify Home schedule filtering behavior from the iOS product before wiring parity UI or analytics. | Active |
| GAP-018 | Home Assumption | Home mood selection currently models one effective mood choice per date by reusing the first observed same-day mood entry id, and Home snapshot state also uses the first observed same-day mood token unless no entry exists and a deterministic date-scoped id must be created. | The Home mood state is deterministic, but it depends on repository ordering and may differ from the real product if multiple moods per day are supported. | Confirm iOS Home mood-entry rules and ordering semantics before expanding or changing mood interaction behavior. | Active |
| GAP-019 | Home Assumption | When `allowsMultipleUpdatesPerDay` is `false`, Home treats boolean habits as one-tap-per-day no-op after the first entry and quantity habits as single-entry-per-day replacement using a date-scoped entry id. | Home quick actions now honor the existing domain flag, but exact repeat-tap behavior is still provisional. | Verify repeat-update semantics from the iOS product before final UI wiring. | Active |
| GAP-020 | Home Assumption | Home greeting state currently derives `MORNING`, `AFTERNOON`, and `EVENING` from local-hour buckets `5..11`, `12..16`, and all remaining hours because no sourced greeting-time rule exists in the repo. | Greeting logic is deterministic and testable, but the actual product buckets and copy behavior may differ. | Confirm greeting time-of-day rules from the iOS Home screen before wiring the ViewModel into presentation. | Active |

## Update Rule
- Add a new row whenever a session uncovers missing information that can affect implementation.
- Close a row only when the blocking information is actually present in the repository or explicitly approved in durable project docs.
